# Frontend demo app environment variables
#
# Usage:
#   cd frontend
#   cp env.example .env.local
#   # edit .env.local

# Optional RPC overrides for reads + wagmi transports
NEXT_PUBLIC_ANVIL_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# Optional: run the demo in AA-only mode (no wallet UI, focus on gasless flow)
# NEXT_PUBLIC_AA_ONLY_DEMO=1

# -----------------------------
# Optional: Gasless demo (Milestone 6B)
# -----------------------------
#
# The demo UI can trigger `opensub-aa subscribe --sponsor-gas ...` via a Next.js API route.
# These vars are SERVER-SIDE ONLY (do NOT prefix with NEXT_PUBLIC).
#
# You must also build the AA binary once:
#   cargo build --release --manifest-path aa-rs/Cargo.toml

# Optional override: if not set, the CLI reads deployments/base-sepolia.json
# OPENSUB_AA_RPC_URL=https://sepolia.base.org

# Required for the gasless demo route:
OPENSUB_AA_BUNDLER_URL=
OPENSUB_AA_ENTRYPOINT=0x0000000000000000000000000000000000000000
OPENSUB_AA_FACTORY=0x0000000000000000000000000000000000000000

# Alchemy Gas Manager paymaster (Base Sepolia):
OPENSUB_AA_PAYMASTER_URL=https://base-sepolia.g.alchemy.com/v2/<ALCHEMY_API_KEY>
OPENSUB_AA_GAS_MANAGER_POLICY_ID=

# Optional: bump maxFeePerGas/maxPriorityFeePerGas (basis points, 10000 = 1x).
# OPENSUB_AA_GAS_MULTIPLIER_BPS=20000
